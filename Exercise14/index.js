"use strict";
// store the names of people you want to invite in an array
const guestList = ["Muqadass", "Zainab", "Maryam"];
// print a message inviting each person to dinner
for (let i = 0; i < guestList.length; i++) {
    console.log(`Dear ${guestList[i]}, you are cordially invited to dinner.we would be 
    honored to have your presence.`);
}
